A lib/ directory must be created in the current directory
and the following librairies must be added in it:
jms.jar, jndi.jar, jta-spec1_0_1.jar, xerces.jar, jakarta-regexp-1.2.jar

Edit and execute joramConfig.sh:
 - source joramConfig.sh

Recompile the sources under src/:
 - make all


Testing JORAM:
src/fr/dyade/aaa/joram/tests
src/fr/dyade/aaa/joram/demo/commerce

See the READMEs and JORAM website.
